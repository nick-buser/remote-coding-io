import CoreGraphics

extension Theme {
    /// Spacing scale from `docs/feature_plans/10-design-system.md`.
    /// Reach for these instead of literal point values inside layouts.
    enum Spacing {
        static let s1: CGFloat = 4
        static let s2: CGFloat = 8
        static let s3: CGFloat = 12
        static let s4: CGFloat = 16
        static let s5: CGFloat = 24
        static let s6: CGFloat = 32
    }
}
